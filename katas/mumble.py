def accum(s: str) -> str:
    first_letter = s[0].upper()
    letters = s[1:].lower()

 def __next__(self):
    if self.a <= 20:
      x = self.a
      self.a += 1
      return x
    else:
      raise StopIteration
